"""Unified tool dispatcher.

Registers vendor-agnostic tools that dispatch to the correct NOS backend
based on each node's nos_type. Populated in Phase 3.
"""

# Phase 3: implement register_unified_tools(mcp, nodes, registry)
